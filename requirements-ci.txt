# CI用の最小依存（import src.app.main と pytest tests に必要なもののみ）。
# requirements.txt はローカル環境の全量スナップショット（GPU版paddle等の巨大依存を含む）のため
# CI では使用しない。paddle/easyocr/cv2/ultralytics は遅延importのためCIでは不要。
--extra-index-url https://download.pytorch.org/whl/cpu
fastapi==0.136.1
starlette==1.0.0
httpx==0.28.1
python-multipart
pydantic
numpy
pillow==12.2.0
matplotlib
scikit-learn
PyYAML
torch
torchvision
# TrOCR推論コア（services/trocr_engine.py）のテスト用。実モデルはダウンロードせず、
# transformers自体（コードライブラリ）のみをCIへ導入し、from_pretrained()等はテスト側でmonkeypatchする。
transformers==5.14.1
